node_modules/
.next/
out/
build/
dist/

.env
.env.local
.env*.local
.env.production

# Copias de la base que genera el compose de producción.
copias/

uploads/

# Certificado autofirmado que genera `npm run dev:celular`.
certificates/

*.log
npm-debug.log*

.DS_Store
Thumbs.db

next-env.d.ts
.vercel
tsconfig.tsbuildinfo

# Respaldos: el registro de lo que fue pasando y la carpeta de reserva que se
# usa sólo cuando la nube no está disponible. Son de esta máquina, no del
# proyecto.
respaldo.log
respaldos/
