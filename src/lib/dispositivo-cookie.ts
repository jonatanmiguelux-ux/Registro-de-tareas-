/**
 * El nombre de la galleta que fija un celular a una cuadrilla.
 *
 * Vive solo, sin importar nada, porque lo necesitan dos mundos que no pueden
 * compartir código: el proxy (que corre en el borde, sin acceso a la base) y
 * la lógica del dispositivo (que sí la usa). Un simple texto que los une.
 */
export const COOKIE_DISPOSITIVO = "cuadrilla-dispositivo";
